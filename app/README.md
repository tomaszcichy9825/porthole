# Porthole app

Expo (React Native) client. See the repo root README and `docs/spec.md`.

Native video modules (libVLC) need a dev build — Expo Go will fall back to snapshot polling:

```sh
npm install
npx expo run:ios        # or run:android
```
